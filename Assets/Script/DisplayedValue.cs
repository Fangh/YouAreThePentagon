using UnityEngine;
using System.Collections;

public abstract class DisplayedValue : MonoBehaviour {

    abstract public int value();

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
